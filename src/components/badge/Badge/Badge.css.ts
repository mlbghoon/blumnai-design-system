// Badge size constants (for reference, using Tailwind classes in component)
export const badgeHeights = {
  sm: 20,
  lg: 24,
} as const;

// Badge padding constants (for reference, using Tailwind classes in component)
export const badgePadding = {
  sm: {
    horizontal: 8,
    vertical: 4,
  },
  lg: {
    horizontal: 10,
    vertical: 6,
  },
} as const;
