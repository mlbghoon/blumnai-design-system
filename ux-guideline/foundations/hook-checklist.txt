gap → ds-gap-* (not gap-*) | shadow → only shadow-card/shadow-modal-sm/md/lg (no shadow-sm/md/lg/custom box-shadow) | hover → transition-colors (never transition-all) | colors → bg-default/subtle/muted/card (no bg-white/bg-gray-*) | radius → rounded-sm(6) terminal, rounded-md(8) container, rounded-lg(12) content block | weight → font-normal/medium/semibold only (no font-bold/light/extrabold) | spacing → padding-*/width-*/height-* (no p-4/w-16/h-16) | typography → size-sm/md/lg (no text-sm/text-lg) | components → DS components first, never custom when DS has it | tooltip → only on icon-only buttons, truncated text; not when label is sufficient | list separation → gap OR divider, never both
